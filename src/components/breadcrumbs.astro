---
const { links } = Astro.props;
---

<nav>
  <ol>
    {
      links.map((link) => (
        <li class="breadcrumb">
          <a href={link.url}>{link.text}</a>
        </li>
      ))
    }
  </ol>
</nav>

<style>
  nav {
    display: flex;
    flex-direction: row;
    gap: 8px;
    font-size: 14px;
    color: #ccc;
    font-weight: 100;
  }

  nav ol {
    display: flex;
    list-style: none;
    padding: 0;
    margin: 0;
    align-items: center;
  }

  nav li.breadcrumb a {
    text-decoration: none;
  }

  nav li.breadcrumb + li::before {
    display: inline-block;
    margin: 0 0.4em 0 0.9em;
    transform: rotate(16deg) translateY(4px);
    border-right: 0.1em solid currentcolor;
    height: 1.2em;
    content: "";
  }
</style>
