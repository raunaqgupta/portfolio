---
const { title } = Astro.props;
---

<section>
  <h2>{title}</h2>
  <slot />
</section>

<style>
  section {
    border: 1px solid #ccc;
  }

  section > :first-child {
    margin-bottom: var(--space-2);
  }

  section h2 {
    margin: 0;
  }
</style>
