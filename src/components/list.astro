---
const { parent, list } = Astro.props;
---

<ul>
  {
    list
      .sort((a, b) => b.data.date.getTime() - a.data.date.getTime() )
      .map((list) => (
        <li>
          <a href={`/${parent}/` + list.slug}>{list.data.title}</a>
        </li>
      ))
  }
</ul>
