---
const { list } = Astro.props;
---

<ul>
  {
    list
      .sort((a, b) => a.data.title.localeCompare(b.data.title))
      .map((list) => (
        <li>
          <a href={"/projects/" + list.slug}>{list.data.title}</a>
        </li>
      ))
  }
</ul>
