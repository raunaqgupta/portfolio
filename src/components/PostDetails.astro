---

---

<section class="post-details">
  <slot />
</section>

<style>
  .post-details {
    flex: 1;
    min-width: 0;
    max-width: 100%;
    display: flex;
    align-items: center;
    gap: 0.25rem;
    color: var(--color-body-secondary);
    font-size: var(--typography-body-small);
  }
</style>
