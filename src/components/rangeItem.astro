---
const { title, org, from, to } = Astro.props;
---

<section>
  <h3>{title}</h3>
  <div>{org}</div>
  <time datetime={from}>{from}</time> - {
    to ? (
      <time datetime={to}>{to}</time>
    ) : (
      <time datetime={new Date().getFullYear().toString()}>
        {new Date().getFullYear()}
      </time>
    )
  }
  <div></div>
  <slot />
</section>

<style>
  section {
    margin-bottom: 2em;
  }

  time {
    color: #666;
  }
</style>
