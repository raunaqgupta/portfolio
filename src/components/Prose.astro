---

---

<div class="prose">
  <slot />
</div>

<style>
  .prose :global(p),
  .prose :global(h2),
  .prose :global(h3),
  .prose :global(h4),
  .prose :global(ul),
  .prose :global(ol),
  .prose :global(pre),
  .prose :global(blockquote) {
    max-width: 800px;
    margin-inline: auto;
  }
</style>
